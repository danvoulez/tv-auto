{
  "name": "vvtv-crawler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Allowlist-only VVTV discovery crawler using Crawlee + Playwright",
  "scripts": {
    "start": "node run.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "crawlee": "^3.11.4"
  }
}
